import 'package:flutter/material.dart';

class CustomWidget extends StatelessWidget {
  CustomWidget.named({required void Function() onTap});

  @override
  Widget build(BuildContext context) => Container();
}
