// A `part of` file: it belongs to feature.dart and is never a standalone unit,
// so unused-files must not flag it even though nothing imports it directly.
part of 'feature.dart';

void featurePartHelper() {
  print('part');
}
