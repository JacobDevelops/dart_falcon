/// See [List<int>] for details.
/// The [Map<String, int>] returned is unmodifiable.
/// Delegates to [Future<void>] and then to [Stream<Event>].
/// A [Set<String>] reference followed by prose about a < b.
/// Renders a <keygen> control in the generated form.
/// Mixed: `Iterable<T>` in code and [Iterable<T>] as a reference.
class Api {}
